/**
 *
 * @param {string} name
 * @returns {string}
 */

function greeting(name) {
  if (name === 'Mubashir') {
    return 'Hello, my Love!';
  } else {
    return 'Hello, ' + name + '!';
  }
}

export default greeting;
